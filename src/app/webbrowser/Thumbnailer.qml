import QtQuick 2.4
import Ubuntu.Thumbnailer 0.1

/* Because Thumbnailer isn't in the main repository the webbrowser can't
 depend on it. We use this file to dynamically load the thumbnailer image
 provider if it's installed. */

Item { }
